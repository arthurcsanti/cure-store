# Curada — mockup do site

Site estático (HTML/CSS puro, sem build) com o layout da loja de curadoria de moda:

- `index.html` — home
- `colecao.html` — vitrine da coleção
- `produto.html` — página de produto
- `style.css` — estilos compartilhados

Ainda é um mockup visual (fotos são placeholders). Não tem carrinho/checkout real —
isso continua vindo da plataforma de loja (Shopify/Nuvemshop) quando a loja de verdade existir.
